services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: her
      POSTGRES_PASSWORD: her_dev
      POSTGRES_DB: her_db
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  her_pgdata:
      